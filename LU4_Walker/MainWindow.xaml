<Window x:Class="LU4_Walker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LU4 Walker" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Выберите процесс LU4:" FontSize="14" Margin="0,0,0,10"/>
        <ComboBox Grid.Row="1" x:Name="ProcessComboBox" Width="360" SelectionChanged="ProcessComboBox_SelectionChanged" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="StartButton" Content="Старт" Width="100" Margin="5" Click="StartButton_Click"/>
            <Button x:Name="StopButton" Content="Стоп" Width="100" Margin="5" IsEnabled="False" Click="StopButton_Click"/>
        </StackPanel>

        <!-- Overlay для рисования -->
        <Canvas x:Name="OverlayCanvas" IsHitTestVisible="False"
                Background="Transparent" Panel.ZIndex="999"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</Window>
